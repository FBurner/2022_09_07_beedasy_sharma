{% extends "base.html.twig" %}

{% block content %}
    {% for post in posts %}
        <a href="/posts/{{ post.post_id }}">{{ post.title }}</a>
        {{ post.body }}
    {% endfor %}
{% endblock %}