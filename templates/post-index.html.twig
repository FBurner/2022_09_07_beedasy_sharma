{% extends "base.html.twig" %}

{% block content %}
    {% for post in posts %}
        <div>
        <h1><a href="/posts/{{ post.post_id }}">{{ post.title }}</a></h1>
        {{ post.body }}
        </div>
    {% endfor %}
{% endblock %}